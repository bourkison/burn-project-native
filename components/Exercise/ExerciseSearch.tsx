import React from 'react';
import {Text} from 'react-native-elements';

const ExerciseSearch = () => {
    return <Text style={{color: '#f3fcf0'}}>Exercise Search.</Text>;
};

export default ExerciseSearch;
